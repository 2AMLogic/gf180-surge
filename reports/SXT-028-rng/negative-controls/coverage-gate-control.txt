SXT-029 negative controls — coverage pipeline (issue #22 acceptance)
runner: tools/coverage_negative_controls.py; scratch root: /tmp/sxt029-negative-controls (recreated each run; published outputs never touched)
controls are healthy when the PIPELINE fails as designed: a stale or missing leaf evidence must DOWNGRADE affected presets from supported to unresolved (never report them supported); a corpus entry without a coverage row must REFUSE fail-closed.
SUBSET RUN: only ['rng_exclusion']

NC-RNG-EXCLUSION: FX-RNG exclusion gate (#122/DR-0013)
    baseline supported                  : 1681
    synthetic exclusions added          : 5
    after gate supported                : 1676
    downgraded supported->unresolved    : 5 (all carry fx_rng_gate=BLOCKED and an rng_stream_unpinnable: reason)
    with --control-ignore-rng-exclusion : 1681 (exactly the baseline -- the RNG gate, and nothing else, caused the downgrade)
    denominator unchanged               : 3561 (reduction published, nothing dropped)
    PASS: affected presets downgraded; gate proven load-bearing; denominator preserved

RESULT: PASS — all controls healthy (each demonstrably fails the check it targets)
